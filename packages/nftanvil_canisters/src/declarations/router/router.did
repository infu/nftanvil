type Router = 
 service {
   debug_reset: () -> ();
   fetchNFTCan: (nat) -> (text) query;
   fetchSetup: () -> (record {
                        access: text;
                        acclist: vec text;
                      }) query;
   getAvailable: () -> (principal) query;
   reportOutOfMemory: () -> ();
 };
service : () -> Router
