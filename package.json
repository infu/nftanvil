{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "watch": " NODE_ENV=production lerna run watch --parallel",
    "build": " NODE_ENV=production lerna run build --parallel",
    "bootstrap": "lerna bootstrap"
  },
  "npmClient": "yarn",
  "dependencies": {
    "@babel/cli": "^7.16.0",
    "@babel/core": "^7.17.8",
    "@babel/node": "^7.16.0",
    "@babel/preset-env": "^7.16.11",
    "yarn-audit-fix": "^9.2.0"
  }
}
